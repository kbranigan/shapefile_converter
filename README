This C application converts shapefile (.dbf, .shp and .shx files) to mysql .sql dump files

http://en.wikipedia.org/wiki/Shapefile

Just run 'make' and run the executable - it's written and only tested on Mac OS X.